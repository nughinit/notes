# 7. Fechar exceção legada de `monitor_run` em `TRT3` e `TRT10`

- [ ] Entender por que `TRT3`/`TRT10` estão na lista de
      `l8_legacy_monitor_run_exception` (provavelmente `monitor_run` de formato
      antigo)
- [ ] Rodar o coletor L8 atual para os dois tribunais e gerar `monitor_run` no
      formato novo
- [ ] Remover a exceção legada depois de validado
