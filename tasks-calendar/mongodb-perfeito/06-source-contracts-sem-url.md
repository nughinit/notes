# 6. Resolver os 50 `source_contracts` sem URL (exceção legada)

- [ ] Listar os 50 `contract_*` sem `url` marcados como
      `source_contract_missing_url_legacy_exception`
- [ ] Para cada um, localizar a fonte real (site do tribunal) e preencher a
      `url`
- [ ] Onde a fonte não existir mais, marcar o contrato como `deprecated` em
      vez de manter a exceção
- [ ] Remover a exceção legada da auditoria após todos preenchidos
